<div class="mt-5">
  <%= simple_form_for :scrape_release_id, url: products_path, method: :post do |f|  %>
    <%= f.input :release_id, placeholder: "Paste your discogs release URL or release ID", label: false %>

    <%= f.submit "Import", class: "btn btn-primary btn-lg btn-block" %>
  <% end %>
</div>
